plugins {
    id("musicplayer.android.library")
}

android {
    namespace = "com.andanana.melodify.core.domain"

    defaultConfig {
        testInstrumentationRunner = "androidx.test.runner.AndroidJUnitRunner"
    }
}
